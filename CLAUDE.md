# CLAUDE.md — kumamoto-tsunagaru

この案件（kumamoto-tsunagaru）専用の指示ファイル。作業前に [PROJECT.md](PROJECT.md) を確認すること。

# 対話型ヒアリング・プロトコル（全プラグイン共通の参照）

> このファイルは各 skill が実行時に読み込む共通規律です。プロジェクト/組織の CLAUDE.md に同じ節があればそちらを優先します。

## 対話型ヒアリング・プロトコル（全フェーズ共通）

フェーズ系 skill（要件受領・見積り・設計・テスト・リリース等）を実行するときは、
**いきなり成果物を作らず**、まず逆質問で理解を詰めてから確定する。動作は次の 2 パス。

### Pass 1 ── 理解と質問（成果物は出さない / ここで一旦止まる）
1. 入力（テキスト・ファイル・前フェーズ成果物・`PROJECT.md`）を読み、
   **「私の理解」を 3〜6 行で要約**して提示する。
   - 入力は **議事録・動画文字起こし・複数の添付ファイル（Excel/PDF/Word/画像）・要件テキスト・口頭** が混在しうる。
     **推測で進めず、与えられた素材を実際に開いて全部読む**。
   - 何を作るか（**システム類型**）を見極め、型に応じて確認すべき構造を変える（決め打ちしない／特定サンプルに寄せない）。
2. 次の 5 レンズで確認・提案点を洗い出す：
   - **解釈**：私の理解で合っているか（"つまりこういうものを作りたい、で正しいですか？"）
   - **抜け漏れ**：この種の案件に一般的に含まれるが入力に無い要素（"通常◯◯が含まれますが不要ですか？"）
     - 各 skill の固定チェックリストを**最低ライン**として必ず確認する。
     - **それに加えて**、「この案件・このドメインで一般的に必要なのに、まだ要件化されていないもの」を
       **能動的に洗い出して**質問に含める（固定リストを天井にしない）。気づいた標準は黙って実装せず必ず確認に出す。
   - **付加価値**：顧客満足が上がりそうな提案（"◯◯にすると顧客はより満足しそうですが入れますか？"）
   - **前提・制約**：見積り/設計の前提として確定が要るもの
   - **リスク**：放置すると後で揉める論点
3. それらを **1 つの質問票**にまとめて提示する。各項目は必ず以下を含める：
   `# / 観点タグ / 質問文 / AI の推奨（デフォルト） / 回答方法（Yes・No／選択／記述） / 重要度（★必答・任意）`
4. **★必答の数は絞らない。品質を確定するために必要なだけ出す**（超最高品質を優先）。質問は重要度順に並べる。残りは任意。
   - 数が増えても負担にならないよう、**各項目に必ず「AI の推奨（デフォルト）」を付け、末尾の一括 OK で全承認できる**形を保つ。
   - 観点ごと（解釈／抜け漏れ／付加価値／前提／リスク）に小見出しで束ね、重要なものから並べる。
5. 開発者では確定できず**顧客確認が要る項目**は「**顧客確認事項**」として別枠に列挙する。
6. 質問票の末尾に必ず一括回答の案内を付ける：
   > すべて推奨通りでよければ「**OK**」とだけ返してください。
   > 変えたい項目は番号で指定（例：「3＝不要、5＝B 案に」）。顧客確認事項は「未定」で構いません。
7. **ここでターンを終える。構造化成果物・見積り等はまだ出さない。**

### Pass 2 ── 確定とインプット（開発者の回答を受けてから）
1. 回答（OK／個別修正／追記）を反映する。
2. **未回答の★必答が残っていれば、その項目だけ再提示して再び止まる**。
3. 全確定後に、当該フェーズの構造化成果物を **skill 指定の出力フォーマット**で生成する。
4. 「未定／顧客確認事項」は成果物内に **明示的に残す**（握りつぶさない・勝手に確定しない）。
5. ★HITL ゲート（金額・スコープ・設計・リリース）のフェーズは、
   成果物提示後に **承認を求めて再度止まる**。

### 守ること（全フェーズ共通）
- AI は「叩き台の生成」と「抜け漏れ・矛盾の検知」に徹する。**採否・意思決定は人間が行う。**
- **確認と提案を繰り返して組み上げる。** 1 回の質問票で終わらせず、回答反映→新たな論点を再提示、を確定まで反復する。
- **推測で埋めない。** やむを得ず埋めた箇所は `【仮置き】` と明記する。
- **同じ質問を繰り返さない。** `PROJECT.md` で確定済みの事項は再質問しない。
- 各フェーズの開始時に `PROJECT.md` を読み、終了時に確定事項・未確定・承認記録を追記する。
- **確認の上で採用した「要件外の標準項目」は必ず記録に残す**：
  - 機能になるもの（認証・監査ログ・バックアップ・検索・エクスポート等）は `REQ-001` 形式の **REQ-ID** を振る。
  - 機能要件にならない標準非機能（性能・セキュリティ・可用性・入力検証・エラー処理・ログ等）は `NFR-001` 形式の **NFR-ID** を振る。
  - 「一般的にこうだろう」で確認の上に採用した想定は、**NFR/REQ 化して以降のフェーズが追跡できる**状態にする（実装したのに無記録、を作らない）。
- **未確認の想定（仮置き）は「仮置き台帳」で管理し、想定の上に勝手に実装しない**：
  - `【仮置き】` は `PROJECT.md` の **仮置き台帳**（未確認の想定・確認先・確認期限）に必ず登録する。
  - **アーキ・スコープ・金額に影響する仮置きの上に設計・実装を進めない**。進める場合は影響を明記し、開発者（必要なら顧客）の合意を得てから。
- 各フェーズは **定義された成果物を実ファイルとして産出**し、**完成条件（DoD）を満たすまで完了にしない**。
  DoD を満たしたら保存先パスへ書き出し、`git commit` する（コミットメッセージにフェーズ番号と REQ/NFR-ID）。
- 要件・標準項目は ID（REQ/NFR）で串刺しし、以降のフェーズ（設計・実装・テスト・受入）は ID で参照する。
